# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLOv8 object detection model with P2-P5 outputs. For Usage examples see https://docs.ultralytics.com/tasks/detect

# Parameters
nc: 3  # number of classes
scales: # model compound scaling constants, i.e. 'model=yolov8n.yaml' will call yolov8.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [1, 1.0, 128]

# YOLOv8.0 backbone

backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [8, 3, 2]]                             # 0
  
  - [-1, 1, DWConv, [16, 3, 2]]                            # 1
  - [-1, 1, RepVGGBlock, [16, True]]                     # 2
#   - [-1, 1, CBAM, [16]]                                
  
  - [-1, 1, DWConv, [32, 3, 2]]                            # 3
  - [-1, 1, RepVGGBlock, [32, True]]
#   - [-1, 1, CBAM, [32]]
  
  - [-1, 1, DWConv, [64, 3, 2]]                            # 5
  - [-1, 2, RepVGGBlock, [64, True]]
  - [-1, 1, CBAM, [64]] 
  
  - [-1, 1, DWConv, [128, 3, 2]]                           # 8
  - [-1, 2, RepVGGBlock, [128, True]]
  - [-1, 1, CBAM, [128]] 
  
  - [-1, 1, SPPF, [128, 5]]                              # 11

# YOLOv8.0-p2 head
head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]           # 12
  - [[-1, 6], 1, Concat, [1]] 
  - [-1, 1, RepVGGBlock, [64, True]]
  - [-1, 1, CBAM, [64]] 

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]           # 16
  - [[-1, 4], 1, Concat, [1]] 
  - [-1, 1, RepVGGBlock, [32, True]] 

  - [-1, 1, DWConv, [64, 3, 2]]                           # 19
  - [[-1, 14], 1, Concat, [1]] 
  - [-1, 1, RepVGGBlock, [64, True]] 

  - [-1, 1, DWConv, [128, 3, 2]]                           # 22
  - [[-1, 9], 1, Concat, [1]] 
  - [-1, 1, RepVGGBlock, [128, True]]

#   - [[19, 22, 25], 1, Detect, [nc]]
  - [[18, 21, 24], 1, Detect, [nc]]




# backbone:
#   # [from, repeats, module, args]
#   - [-1, 1, Conv, [32, 3, 2]]  # 0-P1/2
#   - [-1, 1, DWConv, [64, 3, 2]]  # 1-P2/4
#   - [-1, 3, RepVGGBlock, [64, True]]
#   - [-1, 1, DWConv, [128, 3, 2]]  # 3-P3/8
#   - [-1, 6, RepVGGBlock, [128, True]]
#   - [-1, 1, DWConv, [256, 3, 2]]  # 5-P4/16
#   - [-1, 6, RepVGGBlock, [256, True]]
#   - [-1, 1, DWConv, [512, 3, 2]] # 7-P5/32    512
#   - [-1, 3, RepVGGBlock, [512, True]]
#   - [-1, 1, SPPF, [512, 5]]  # 9     512

# # YOLOv8.0-p2 head
# head:
#   - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
#   - [[-1, 6], 1, Concat, [1]]  # cat backbone P4
#   - [-1, 3, RepVGGBlock, [256, True]]  # 12

#   - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
#   - [[-1, 4], 1, Concat, [1]]  # cat backbone P3
#   - [-1, 3, RepVGGBlock, [128, True]]  # 15 (P3/8-small)

#   - [-1, 1, Conv, [256, 3, 2]]
#   - [[-1, 12], 1, Concat, [1]]  # cat head P4
#   - [-1, 3, RepVGGBlock, [256, True]]  # 24 (P4/16-medium)

#   - [-1, 1, Conv, [512, 3, 2]]
#   - [[-1, 9], 1, Concat, [1]]  # cat head P5
#   - [-1, 3, RepVGGBlock, [512, True]]  # 27 (P5/32-large)

#   - [[15, 18, 21], 1, Detect, [nc]]  # Detect(P2, P3, P4, P5)

