# Shared dependencies for all modes.
#
# Choose ONE profile per environment:
# 1) Ultralytics profile:
#    python -m pip install -r requirement.txt -r requirements.ultralytics.txt
# 2) TensorFlow legacy QAT profile:
#    python -m pip install -r requirement.txt -r requirements.qat-tflegacy.txt
#
# Do not install both profiles in the same environment.

numpy>=1.26,<2.0
pandas>=2.1,<3.0
opencv-python>=4.8,<5.0
tqdm>=4.66,<5.0
matplotlib>=3.8,<4.0
pyyaml>=6.0,<7.0
